// // add
// subtract
// multiply
// divide

const add = (a,b) => a + b;
const subtract = (a,b) => a - b;
const multiply = (a,b) => a * b;
const divide = (a,b) => (a / b).toFixed(2);

add(9,2);
divide(9,2);
multiply(9,9);

